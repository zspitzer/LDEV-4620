name: Continuous Integration
on:
  push:
  pull_request:
jobs:
  ci:
    name: Preside CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: dump dir
        run: |
          pwd
          echo "password" > /usr/lib/CommandBox/server/06C699DF3346208DBEBADAC8F62CC045-presidetests-regression/lucee-6.0.0-SNAPSHOT.493/WEB-INF/lucee-server/context/password.txt
      - name: Install dependencies
        uses: pixl8/github-action-box-install@v2
        with:
          verbose: true
          force: true
      - name: Run tests
        uses: pixl8/github-action-box-test@v3
        with:
          boxjson_dir: /tests
          test_dir: /tests
          test_server_json: /tests/server.json
          output_file: /tests/test-results.txt
          verbose: true
